name: CI Quality & Security
on:
  workflow_call:

permissions:        
  contents: read
  packages: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore & Build
        run: |
          dotnet restore app/kleva-app.csproj
          dotnet build app/kleva-app.csproj --configuration Release --no-restore

      - name: Test
        run: dotnet test app/ --no-build

      # ✅ FIX: setup y run son dos steps SEPARADOS, nunca en uno solo
      - name: Setup Snyk
        uses: snyk/actions/setup@master

      - name: Snyk Test
        run: snyk test --file=app/kleva-app.csproj --severity-threshold=high
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # ✅ FIX: versión correcta de SonarCloud
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}